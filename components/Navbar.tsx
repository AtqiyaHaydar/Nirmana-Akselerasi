import Image from 'next/image'
import Hero from '@/public/hero.jpg'

const Navbar = () => {
  return (
    <header className=' h-[65px] flex items-center w-full bg-white/5 backdrop-blur px-20 fixed top-0'>
      Nav
    </header>
  )
}

export default Navbar